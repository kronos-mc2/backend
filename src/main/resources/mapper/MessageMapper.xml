<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hr.kronos.backend.messages.persistence.MessageMapper">

  <select id="findConversations" resultType="hr.kronos.backend.messages.persistence.ConversationRow">
    SELECT
      id,
      contact,
      last_message_hr,
      last_message_en,
      time_label
    FROM conversations
    ORDER BY updated_at DESC, id ASC
  </select>

  <select id="findConversationById" resultType="hr.kronos.backend.messages.persistence.ConversationRow">
    SELECT
      id,
      contact,
      last_message_hr,
      last_message_en,
      time_label
    FROM conversations
    WHERE id = #{id}
    LIMIT 1
  </select>

  <update id="updateConversationSharePreview">
    UPDATE conversations
    SET last_message_hr = #{lastMessageHr},
        last_message_en = #{lastMessageEn},
        time_label = TO_CHAR(NOW(), 'HH24:MI'),
        updated_at = NOW()
    WHERE id = #{id}
  </update>

</mapper>
