<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hr.kronos.backend.auth.persistence.AuthMapper">

  <select id="findByEmail" resultType="hr.kronos.backend.auth.persistence.UserRow">
    SELECT
      id,
      email,
      full_name,
      password_hash,
      auth_provider,
      bio,
      avatar_url,
      created_at,
      updated_at
    FROM app_users
    WHERE email = #{email}
    LIMIT 1
  </select>

  <select id="findById" resultType="hr.kronos.backend.auth.persistence.UserRow">
    SELECT
      id,
      email,
      full_name,
      password_hash,
      auth_provider,
      bio,
      avatar_url,
      created_at,
      updated_at
    FROM app_users
    WHERE id = #{id}
    LIMIT 1
  </select>

  <select id="findBySocialIdentity" resultType="hr.kronos.backend.auth.persistence.UserRow">
    SELECT
      u.id,
      u.email,
      u.full_name,
      u.password_hash,
      u.auth_provider,
      u.bio,
      u.avatar_url,
      u.created_at,
      u.updated_at
    FROM user_social_identities usi
    JOIN app_users u ON u.id = usi.user_id
    WHERE usi.provider = #{provider}
      AND usi.provider_subject = #{providerSubject}
    LIMIT 1
  </select>

  <insert id="insert" parameterType="hr.kronos.backend.auth.persistence.UserRow">
    INSERT INTO app_users (
      id,
      email,
      full_name,
      password_hash,
      auth_provider
    ) VALUES (
      #{id},
      #{email},
      #{fullName},
      #{passwordHash},
      #{authProvider}
    )
  </insert>

  <insert id="insertSocialIdentity">
    INSERT INTO user_social_identities (
      id,
      user_id,
      provider,
      provider_subject,
      email
    ) VALUES (
      #{id},
      #{userId},
      #{provider},
      #{providerSubject},
      #{email}
    )
    ON CONFLICT (provider, provider_subject) DO NOTHING
  </insert>

  <update id="updateSocialIdentityEmail">
    UPDATE user_social_identities
    SET email = #{email},
        updated_at = NOW()
    WHERE provider = #{provider}
      AND provider_subject = #{providerSubject}
  </update>

  <update id="updateName">
    UPDATE app_users
    SET full_name = #{fullName},
        updated_at = NOW()
    WHERE id = #{id}
  </update>

  <update id="updateProfile">
    UPDATE app_users
    SET full_name = #{fullName},
        bio = #{bio},
        avatar_url = #{avatarUrl},
        updated_at = NOW()
    WHERE id = #{id}
  </update>

</mapper>
