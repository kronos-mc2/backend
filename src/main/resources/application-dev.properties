# Development profile for local run.
# Dev-only beans (for example event seeding) are enabled via @Profile("dev").
spring.config.import=optional:file:.env.dev[.properties],optional:file:backend/.env.dev[.properties]
logging.level.hr.kronos.backend.config.DevHttpRequestLoggingFilter=INFO
